import React from 'react';
import ThemedButton from "./ThemedButton";
const Toolbar = (props) =>{
        return(
            <div>
                <ThemedButton />
            </div>
        );
}

export default Toolbar;